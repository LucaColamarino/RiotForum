<%= form_with(model: @messageteam, url:chat_team_new_path(sender: current_user.username)) do |form| %>

        <div class="col-md-6 mx-auto">
              <%= form.label :text, "Messaggio" %><br>
              <%= form.text_area :text, style:'resize:none' %>  
        </div>
        <div class="text-center">
          <%= form.submit "Manda", class: "btn btn-primary" %>
        </div>


<% end %>